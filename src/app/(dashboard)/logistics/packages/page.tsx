import { PackagesView } from "./PackagesView";

export const metadata = {
  title: 'Logistics Shipments | Komtru TrustOps',
  description: 'Monitor cross-company shipments, tracking numbers, and lifecycle transitions.',
};

export default function LogisticsPackagesPage() {
  return <PackagesView />;
}
